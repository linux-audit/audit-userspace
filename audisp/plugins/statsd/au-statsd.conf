# This file controls the configuration of the statsd plugin.
# It picks out metrics and writes them to statsd.

active = no
direction = out
path = /sbin/audisp-statsd
type = always 
# args =
format = string
